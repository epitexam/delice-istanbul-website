---
interface Props {
  number: any;
  label: any;
  variant?: 'default' | 'red';
}
const { number, label, variant = 'default' } = Astro.props;
---
<div class={`stat stat--${variant}`}>
  <div class="stat-num">{number}</div>
  <div class="stat-label">{label}</div>
</div>

<style>
.stat {
  padding: clamp(12px, 2vw, 16px) clamp(14px, 2vw, 22px);
  background: var(--color-ink);
  color: var(--color-cream);
  min-width: min(108px, 100%);
}
.stat--red {
  background: var(--color-red);
}
.stat-num {
  font-family: var(--font-family-display);
  font-size: 30px;
  line-height: 1;
  letter-spacing: .01em;
}
.stat-label {
  font-size: 10px;
  font-weight: 600;
  letter-spacing: .2em;
  text-transform: uppercase;
  color: var(--color-accent-on-dark);
  margin-top: 6px;
}
.stat--red .stat-label {
  color: var(--color-text-on-red);
}
</style>